# MySQL connection settings — copy this file to .env and fill in real values.
# NEVER commit the real .env file (it is in .gitignore).
# These are read by src/config.py from the local .env / process environment.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=bia_fraud_detection
DB_USER=root
DB_PASSWORD=
